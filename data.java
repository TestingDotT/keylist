// Data

public class data {
        public static void main(String[] args) {
                // Key list (Updated)
                // Index data
                String[] keylist = {"key1", "key2", "key3", "key4"};
                
                // Check
                if (userkey.equals(keylist[0])) {
                        // Output test
                        System.out.println("Welcome!");
                } else {
                        System.out.println("Error. Not welcome!");
                }
        }
}
